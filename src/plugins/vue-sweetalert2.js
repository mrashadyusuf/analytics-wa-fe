import swal from 'vue-sweetalert2'

const swalOptions = {
  customClass: {
    closeButton: 'v-btn v-btn--size-default',
    confirmButton: 'v-btn v-btn--size-default',
    denyButton: 'v-btn v-btn--size-default',
    cancelButton: 'v-btn v-btn--size-default',
  },
}

export { swal, swalOptions }