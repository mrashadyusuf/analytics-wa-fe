import swal from 'vue-sweetalert2'

const swalOptions = {
  customClass: {
    closeButton: 'v-btn',
    confirmButton: 'v-btn',
    denyButton: 'v-btn',
    cancelButton: 'v-btn',
  },
}

export { swal, swalOptions }